# DoriDori パネルUIの設計メモ

初期検討: 2026-03-20。実装との照合: 2026-09-05。

この文書は現行実装と今後の設計案を区別して記録する。
初期のHomeTeacherProtoは単一HTMLのモックだったが、現在は `repos/doridori-app` のReactアプリを使用する。
ルートの `index.html` は旧モックとして残し、GitHub Pagesのデプロイ対象には含めない。

## 現行実装

```text
PDF
  → 範囲を囲む
解答記入（切り抜き画像にペン書き）
  → ツールバーから送信
採点結果
  → 結果の一部を囲む
質問記入（切り抜き画像にペン書き）
  → 通常の採点APIへ再送信
採点結果
  → 繰り返し
```

- `StudyPanel.tsx` の `panelStack` が `pdf / answer / grading` のパネルを管理する。
- 質問記入は `answer` パネルに `source: 'grading'` を付けて表現する。
- パンくずで任意のパネルへ戻れる。途中から新しいパネルを追加すると、それより先の分岐は置き換わる。
- パネル履歴はReactのメモリ内にあり、会話セッションとして永続化していない。
- PDFはA/Bページ切替・左右分割に対応し、スワイプやパン操作はPDFの操作に使う。
- 共通Admin画面と採点結果にはSNSリンク・利用時間設定が残っている。

## AIへ送信している情報

`StudyPanel.confirmAndGrade` → 共通 `gradeWork` → `POST /api/grade-work` の順に呼び出す。
送信するのは、その時点の切り抜き画像とモデル・言語などの採点パラメーター。
質問記入からの送信も同じ経路であり、質問専用の会話プロンプトへは切り替わらない。

過去のやり取り、最初の問題画像、PDF全ページは自動では送られない。
画面上のパンくずの履歴と、AIに渡す会話履歴は別のもの。

## 今後の設計案（未実装）

### 対象と方向性

中高生の「なぜ間違えたか」「もっと詳しく」「類題を出して」に応える深掘り学習と、
本を読みながら範囲を囲ってAIと対話する体験を目指す。
現在の採点プロンプト・管理画面には、TutoTuto由来の小中学生向け採点やSNS機能が残る。

### 対話専用APIと会話履歴

初期案のB方式は「最初に画像を送り、以後は会話履歴を保持する」方式。
画像を毎回保持するA方式も候補だが、どちらも現行APIに組み込まれていない。
実装時は質問・回答の型、会話の保存範囲、途中のパンくずから分岐した場合の履歴の扱いを定義する。

### 本全体のコンテキスト

PDF全体をAIへ渡して読書中の対話に利用する案。
プレミアム機能とする構想はあるが、読み込み・コンテキスト管理・利用制限・課金による解放は未実装。

### SNS機能の整理

初期案ではDoriDoriにSNSリンク管理を含めない方針だった。
現行実装では利用可能なため、削除済みとは扱わない。
将来除外する際は、共有コンポーネントにアプリ別の設定を設け、TutoTutoのSNS報酬機能への影響を避ける。
